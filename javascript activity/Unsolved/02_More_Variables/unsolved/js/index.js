// Create the addNumbers function here.
function addNumber() {
  var two = 2;
  var three = 3;
  var five = two + three;
  console.log(five);
}
addNumber(); // Dont put this into function addNumber because 'addNumber' is already inside function

// Create the stringConcat function here.
function stringConcat() {
  var taco = "taco";
  var cat = "cat";
  var tacoCat = "taco" + "cat";
}
alert("tacoCat");
springConcat();
